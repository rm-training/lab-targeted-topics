const name = 'Abe';
const age = 101;

console.log(
  name + ' is ' + age + ' years old'
);

// We can also run our strings through a function
// using a tag function
//
// function convertStringAge(str, name, age) {
//   const ageString = age > 100 ? 'super old' : age;
//   return `${name}${str[1]}${ageString}`;
// }

// console.log(convertStringAge`${name} is ${age} years old`);
